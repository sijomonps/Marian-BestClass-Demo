# Roles Folder

Each user role has its own folder with:
- role-specific entry HTML pages
- small page bootstrap config scripts
- `USER.md` for role usage reference

Folders:
- `student/`
- `teacher/`
- `evaluator/`
- `admin/`
- `hod/`
